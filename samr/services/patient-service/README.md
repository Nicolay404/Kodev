# Patient Service

Servicio del módulo M1 encargado de gestionar el perfil clínico del paciente.

## Ejecución Local

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 8002
```
